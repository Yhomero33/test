/**new Rect(10,10,100,100)
.addTo(stage)
.attr('fillColor', 'green')
/**.animate("10s",{
    x: 500,
    y:200
})*/
/*    .animate(new KeyframeAnimation('2s',{
        from: {x: 0, y: 0}, 
        '0.5%':{x:300, y:300}, 
        to: {x:0, y:0}
    }));

    new Circle(500,500,100)
    .addTo(stage)
    .attr('fillcolor','yellow');
*/
    new Circle(500,500,100)
    .addTo(stage)
    .attr('fillcolor','blue');

    const x=200, y=300, r=50, 
    let = ai=Math.PI.Math, af=Math.PI*0.5, scr=false;
    new arc(x, y, radio, ai, af, scr)
    .addTo(stage)
    .attr('fillcolor','blue');


/**
 * Equivalente en html, sin llamar documento 
  code: function(){-->
    new Rect(10,10,100,100)
    .addTo(stage)
    .attr('fillColor', 'green')
},*/